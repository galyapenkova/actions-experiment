name: Workflow testing
on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  experimental-great:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the User
        run: echo "Hello World!"
      - name: Show directory checkout
        run: ls -l
      - name: Checkout our code
        uses: action/checkout@v2
      - name: Show directory after checkout 
        run: ls -l
